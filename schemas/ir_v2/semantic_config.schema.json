{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/ir_v2/semantic_config.schema.json",
  "title": "Semantic Slot Config",
  "description": "semantic slot config — semantic analysis parameters (reserved, 定稿 S10.3)",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "embedding_model": {
      "type": "string",
      "default": "all-MiniLM-L6-v2",
      "description": "Semantic embedding model for similarity computation"
    },
    "similarity_threshold": {
      "type": "number", "minimum": 0.0, "maximum": 1.0, "default": 0.7,
      "description": "Cosine similarity threshold for semantic segment grouping"
    },
    "topic_model": {
      "type": "string", "default": "default",
      "description": "Topic classification model (reserved)"
    },
    "boundary_detection": {
      "type": "boolean", "default": false,
      "description": "Enable semantic boundary detection for topic-aware segmentation"
    },
    "min_segment_duration": {
      "type": "number", "minimum": 0.5, "maximum": 10.0, "default": 2.0,
      "description": "Minimum segment duration for semantic analysis (seconds)"
    }
  }
}
